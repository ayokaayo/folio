import type { VariantMeta } from '../../types'
import { HERO_PARAMS } from './params'

export const meta: VariantMeta = {
  id: 'moire-wake',
  title: 'Home hero (Wake)',
  author: 'together',
  concept:
    'The production hero (components/hero): two line screens over a physical surface whose fringes flow slowly on their own; moving the pointer disturbs it and waves spread from your path. Defaults are the approved settings in components/hero/settings.ts.',
  params: HERO_PARAMS,
}
